name: Test Minimal

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  call-minimal:
    uses: jasonberkes/taskmaster-shared-workflows/.github/workflows/reusable-auto-rebase-minimal.yml@main
    secrets: inherit
